# Temporary probe, called by probe-crates-auth.yml; deleted after one run.
name: probe-crates-auth-called
on:
  workflow_call:
permissions:
  contents: read
jobs:
  publish:
    if: vars.TABNAS_CRATES == 'on'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: rust-lang/crates-io-auth-action@c6f97d42243bad5fab37ca0427f495c86d5b1a18  # v1.0.5
